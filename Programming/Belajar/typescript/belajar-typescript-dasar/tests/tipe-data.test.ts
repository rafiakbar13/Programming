describe("Tipe Data", () => {
  it("should must declare", () => {
    let name: string = "Rafi";
    let balace: number = 1000000;
    let isVip: boolean = true;
    console.info(name, balace, isVip);
    // name = 100;
    // balace = "1000000";
    // isVip = "true";
  });
});
