"use strict";
describe("Tipe Data", () => {
    it("should must declare", () => {
        let name = "Rafi";
        let balace = 1000000;
        let isVip = true;
        console.info(name, balace, isVip);
        // name = 100;
        // balace = "1000000";
        // isVip = "true";
    });
});
